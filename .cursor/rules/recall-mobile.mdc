---
description: Recall Expo mobile app conventions
globs: apps/mobile/**/*.{ts,tsx}
alwaysApply: false
---

# Recall Mobile (Expo)

~50k TS/TSX excluding tests. Migrated product code lives in `features/<feature>/`; Expo `app/`
files are route adapters. History/search = drawer.

- Functional components + hooks only
- Feature API slices use `lib/api/client.ts` and compose through `lib/api.ts`; never call LLM providers from mobile
- Chat logic in `hooks/useChat*.ts`; fences in `lib/fenceRegistry.ts` + `components/rich/`
- Use product model aliases, never provider names
- Google Sign-In requires dev build; Expo Go uses dev auth placeholder
- Physical device: `EXPO_PUBLIC_API_URL` must be Mac LAN IP, not localhost
- Controls come from `ui/` (see `apps/mobile/ui/README.md`): Button, Chip, ListRow, TextField,
  HeaderButton, Menu/SelectMenu, `confirmDialog`/`alertDialog`, Sheet, the ui/pickers time and
  date dialogs, ShareSheet, Icon. Extend the kit instead of styling a one-off copy in a feature
- Popups: menus are anchored popovers; yes/no questions are `confirmDialog`; passing errors are
  toasts. Bottom sheets only for forms, attach sources and sharing
